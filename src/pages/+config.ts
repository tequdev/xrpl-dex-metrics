import vikeReact from 'vike-react/config'
import type { Config } from 'vike/types'

const config: Config = {
  title: 'XRPL/Xahau DEX Metrics',
  description: 'A dashboard for the XRPL/Xahau DEX',
  extends: [vikeReact],
}

export default config
